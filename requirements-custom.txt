# Dependências externas da customização CKAN SFB
#
# Instalar no venv do CKAN antes de habilitar os plugins:
#
#   source /usr/lib/ckan/venv/bin/activate
#   pip install -e "git+https://github.com/ckan/ckanext-scheming.git#egg=ckanext-scheming"
#
# Observação:
# - ckanext-scheming é dependência externa e não foi copiado inteiro para este repo.
# - O arquivo custom dentro dele, scheming_presets_custom.json, foi preservado em rootfs
#   porque existe na instalação atual e precisa ser revisado/migrado futuramente.
